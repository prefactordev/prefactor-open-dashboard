# Normalise line endings so the repo is identical on Windows, macOS, and Linux.
# Without this, Windows contributors commit CRLF and every diff looks rewritten.
* text=auto eol=lf

# Binary: never touch these.
*.png binary
*.jpg binary
*.ico binary
*.woff2 binary
